---
name: setup
description: Run profile onboarding — collect the user's professional information and populate all profile files so the /apply workflow works out of the box. Use when setting up or re-onboarding the job search framework.
type: prompt
whenToUse: When the user asks to set up, onboard, or populate their candidate profile
---

Thin pointer — the canonical specification is the source of truth. Do not restate it here.

1. Read and follow `.claude/commands/setup.md` exactly, in order. Where the spec references `$ARGUMENTS`, substitute: $ARGUMENTS
