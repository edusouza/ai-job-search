---
name: outcome
description: Record the result of a job application — progress updates (interview invitations, stages, offers) and final resolutions (hired, rejected, no response) into job_search_tracker.csv and the per-application archive. Use when the user reports what happened to an application.
type: prompt
whenToUse: When the user reports an application status change, interview, offer, rejection, or no response
---

Thin pointer — the canonical specification is the source of truth. Do not restate it here.

1. Read and follow `.claude/commands/outcome.md` exactly, in order. Where the spec references `$ARGUMENTS`, substitute: $ARGUMENTS
