---
name: rank
description: Triage scraped jobs into a ranked shortlist — batch-score postings collected by /scrape against the fit framework so the user can decide where to spend /apply effort. Use when the user asks to rank, score, or shortlist scraped jobs.
type: prompt
whenToUse: When the user asks to rank, score, triage, or shortlist collected job postings
---

Thin pointer — the canonical specification is the source of truth. Do not restate it here.

1. Read and follow `.claude/commands/rank.md` exactly, in order. Where the spec references `$ARGUMENTS`, substitute: $ARGUMENTS
