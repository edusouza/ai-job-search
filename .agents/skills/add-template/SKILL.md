---
name: add-template
description: Register a custom CV or cover letter template (LaTeX, Typst, or any CLI-to-PDF toolchain) — store files, capture usage instructions, verify compilation, and wire it into the /apply workflow. Use when the user wants to add or swap a document template.
type: prompt
whenToUse: When the user asks to register, add, or switch a CV or cover letter template
---

Thin pointer — the canonical specification is the source of truth. Do not restate it here.

1. Read and follow `.claude/commands/add-template.md` exactly, in order. Where the spec references `$ARGUMENTS`, substitute: $ARGUMENTS
