---
name: html-report
description: Generate a self-contained HTML dashboard from job_search_tracker.csv and the application archives under documents/applications/ — a single .html file with no server or dependencies. Use when the user asks for a tracker report or dashboard.
type: prompt
whenToUse: When the user asks for an HTML report or dashboard of their job applications
---

Thin pointer — the canonical specification is the source of truth. Do not restate it here.

1. Read and follow `.claude/commands/html-report.md` exactly, in order. Where the spec references `$ARGUMENTS`, substitute: $ARGUMENTS
