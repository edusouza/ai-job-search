---
name: notion-sync
description: Push ranked jobs and applications to a Notion database — a read-only presentation layer over job_scraper/seen_jobs.json and job_search_tracker.csv via the Notion MCP server. Use when the user asks to sync to Notion.
type: prompt
whenToUse: When the user asks to sync their job search data to Notion
---

Thin pointer — the canonical specification is the source of truth. Do not restate it here.

1. Read and follow `.claude/commands/notion-sync.md` exactly, in order. Where the spec references `$ARGUMENTS`, substitute: $ARGUMENTS
