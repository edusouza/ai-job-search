---
name: gmail-sync
description: Scan Gmail for status signals on tracked job applications (interview invites, assessment links, offers, rejections) and, only after user approval, write detected changes into job_search_tracker.csv and outcome.md archives. Use when the user asks to sync application status from email.
type: prompt
whenToUse: When the user asks to check Gmail for application status updates
---

Thin pointer — the canonical specification is the source of truth. Do not restate it here.

1. Read and follow `.claude/commands/gmail-sync.md` exactly, in order. Where the spec references `$ARGUMENTS`, substitute: $ARGUMENTS
