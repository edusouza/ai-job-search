---
name: apply
description: Run the drafter-reviewer job application workflow for a job posting (URL or pasted text) — evaluate fit, tailor the LaTeX CV and cover letter, compile and verify the PDFs. Use when the user asks to apply to a job or tailor application documents.
type: prompt
whenToUse: When the user provides a job posting URL or text and asks to apply, draft a CV/cover letter, or evaluate fit
---

Thin pointer — the canonical specification is the source of truth. Do not restate it here.

1. Read and follow `.claude/commands/apply.md` exactly, in order. Where the spec references `$ARGUMENTS`, substitute: $ARGUMENTS
